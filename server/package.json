{
  "name": "obsidian-code-runner-server",
  "version": "0.0.1",
  "description": "Code runner server",
  "main": "src/server.ts",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.ts",
    "start": "node --env-file=.env src/server.ts"
  },
  "dependencies": {
    "express": "5.2.1"
  },
  "devDependencies": {
    "@types/express": "5.0.6"
  }
}
